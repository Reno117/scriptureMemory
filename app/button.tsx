/*
"use client";

import { createUser } from "./actions";

export function Button() {
  return (
    <button 
      onClick={async () => {
        const r = await createUser();
        console.log("result", r);
      }}
    >
    Create User
    </button>
  );
}

*/